#summary Todo List

  * lua5.1-tests 下 attrib.lua constructs.lua main.lua 无法运行的错误
    || || attrib.lua || constructs.lua || main.lua ||
    || 76 || Y || Y || Y ||
    || 80 || Y || N || Y ||
    || 84 || N || N || N ||

  * ~~F->tables 不应该用 List 看 CloseTable 880~~

  * ~~DeleteFunction 不应该 ClearVpend ，应该在 PrintFunctionCheck 里面检查 vpend是否为空~~